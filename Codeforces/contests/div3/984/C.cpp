#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    /* ios_base::sync_with_stdio(false);
    cin.tie(0); */
    int T = 1;
    cin >> T;
    while(T--){
        string s;
        cin >> s;
        int q;
        cin >> q;
        for(int i = 0; i < s.size(); ++i){
            
        }
        for(int i = 0; i < q; ++i){

        }
    }
    return 0;
}