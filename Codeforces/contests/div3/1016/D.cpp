#include<bits/stdc++.h>
#define ll long long
#define debug(x) cout << #x " = " << x << '\n';
using namespace std;
int main(){
    ll T = 1;
    cin >> T;
    while(T--){
        ll n;
        cin >> n;
        ll q;
        cin >> q;
        while(q--){
            string opt;
            cin >> opt;
            if(opt == "->"){
                ll x, y;
                cin >> x >> y;
                
            }else{
                ll d;
                cin >> d;
                
            }
        }
    }
    return 0;
}