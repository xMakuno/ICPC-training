#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int> distances(101);
    int n, m;
    cin >> n >> m;
    int pos = 1, journey = 1;
    for(int i = 0; i < n; ++i){
        int d, v;
        cin >> d >> v;
        for(j = journey; i < )
    }
    return 0;
}