#include<bits/stdc++.h>
using namespace std;

void constructTree(int input[], int segTree[], int lo, int hi, int pos){
    if(lo == hi){
        segTree[pos] = input[lo];
        return;
    }
    int mid = (lo + hi) / 2;
}

int main(){
    return 0;
}